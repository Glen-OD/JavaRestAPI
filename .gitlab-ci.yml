services:
  - docker:19.03.7-dind

stages:
  - build jar

build:
  image: maven:3.6.3-jdk-11-slim
  stage: build jar
  before_script:
    - source .${CI_COMMIT_REF_NAME}.env
  script:
    - mvn clean install && mvn package -B
  artifacts:
    paths:
      - target/*.jar




    
